- name: Optimize Kernel for High-Traffic Proxy
  sysctl:
    name: "{{ item.name }}"
    value: "{{ item.value }}"
    state: present
    reload: yes
  loop:
    - { name: 'net.core.somaxconn', value: '4096' }
    - { name: 'fs.file-max', value: '2097152' }
    - { name: 'net.ipv4.tcp_fin_timeout', value: '15' }