[ssh_connection]
# This tells Ansible: "Don't connect directly. Use the gcloud IAP tunnel instead."
ssh_args = -o ProxyCommand="gcloud compute ssh --zone=us-central1-a %h --tunnel-through-iap --dry-run"